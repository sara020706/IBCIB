# Backend environment variables

# Host and port the API server listens on
HOST=0.0.0.0
PORT=8000

# Comma-separated list of allowed CORS origins
# Use * to allow all (fine for local dev / internal deployments)
# For production set e.g.: CORS_ORIGINS=https://yourapp.com
CORS_ORIGINS=*
